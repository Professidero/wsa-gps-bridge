name: CI Build & Test

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0.x

    - name: Restore dependencies
      run: dotnet restore WsaGpsBridge.sln

    - name: Build
      run: dotnet build WsaGpsBridge.sln --no-restore --configuration Release

    - name: Run Tests
      run: dotnet test WsaGpsBridge.sln --no-build --verbosity normal --configuration Release